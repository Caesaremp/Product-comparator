{
  "name": "product-comparator",
  "version": "1.0.0",
  "private": true,
  "description": "Local SQLite persistence API for the Product comparator app",
  "scripts": {
    "start:api": "node server/index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^4.21.2"
  }
}
